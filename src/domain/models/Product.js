class Product {
  constructor({ id, name, price, stock, category}) {
    this.id = id; 
    this.name = name; 
    this.price = price; 
    this.stock = stock;
    this.category = category;
  }
}

export default Product;
